import {

} from './constants';

const initialState = {
  // Initial State goes here!
};

const dataViewReducer =
  (state = initialState, action) => {
    switch (action.type) {
      default:
        return state;
    }
  };

export default dataViewReducer;
